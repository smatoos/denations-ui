{
  "name": "denations-ui",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:all": "yarn workspaces foreach -pt run build"
  },
  "devDependencies": {
    "@yarnpkg/pnpify": "^3.0.0-rc.10",
    "typescript": "^4.3.5"
  }
}
